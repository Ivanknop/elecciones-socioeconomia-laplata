# Decisiones metodológicas — panel temporal de ventanas electorales

Registro de las decisiones D1-D13 adoptadas en la rama `especializacion`
(ver `docs/especificaciones/especificacion_panel_temporal.md` para el diseño completo). Cada
decisión indica su estado; las marcadas "pendiente" no habilitan su uso en
conclusiones del TFI hasta resolverse.

| ID | Decisión | Estado |
|---|---|---|
| D1 | Pivote de panel espacial (localidad) a panel temporal de ventanas a nivel distrito | **Pendiente de confirmación formal con la dirección del TFI** |
| D2 | Corte temporal inferior en 2001, por disponibilidad conjunta de series económicas e ICG | Adoptada |
| D3 | El bloque largo (t-2 → t) genera features adicionales de la misma observación, no observaciones nuevas — evita pseudo-replicación | Adoptada |
| D4 | Ventanas de largo homogéneo (~24 meses); no se normaliza por duración variable (único desdoblamiento en 2025, de pocos meses) | Adoptada |
| D5 | Similitud definida como unificación de trayectoria intraventana + posición relativa interventana; se descarta el nivel absoluto como criterio principal | Adoptada |
| D6 | Período 2007-2015 (INDEC intervenido): se incluye, no se excluye a priori. Ver corrección abajo -- ya no se marca con flag | Adoptada (corregida) |
| D7 | Tres series paralelas por nivel, no panel apilado. Pooling parcial entre niveles queda por resolver empíricamente | Adoptada (pooling abierto) |
| D8 | El panel espacial por localidad se conserva como análisis secundario y para visualización, no se elimina | Adoptada |
| D9 | El conjunto de variables es abierto y extensible. Se declara en `registro_variables.csv`, no en código. `features_ventana.py` opera genéricamente sobre ese registro. Incorporar una variable nueva no requiere refactorizar | Adoptada |
| D10 | `panel_ventanas.csv` mantiene las 31 filas con columna `nivel` (un solo pipeline de construcción), pero la interfaz de carga para modelado (`src/ml_models/cargar_panel.py`) exige `nivel` como parámetro obligatorio en `cargar_panel()`. El apilado de los tres niveles requiere `cargar_panel_apilado(justificacion)`, una función separada que exige justificación explícita. La estructura de datos no compromete la resolución de D7: ambas vías (sin pooling, pooling parcial vía apilado deliberado) siguen disponibles, pero el apilado nunca es la ruta por defecto ni puede ocurrir por accidente | Adoptada |
| D11 | `resultado_distrito.csv`/`voto_partido_distrito.csv` usan siempre la etapa `generales` (primera vuelta), nunca `balotaje` -- misma convención que `analisis.graficos._cargar_circuito`. Para presidente 2015 y 2023, `gana_oficialismo`/`share_oficialismo` describen la primera vuelta en La Plata, no el resultado final de la elección (decidido en balotaje). Verificado empíricamente: en ambos años el resultado de primera vuelta en La Plata coincide con el `agrupacion_ganadora`/`era_oficialismo` ya curado en `oficialismos.csv`, así que esto es consistente con el resto del repo, no una fuente nueva de error | Adoptada |
| D12 | `oficialismo_por_nivel.csv`: `agrupacion_oficialismo` es el titular del Ejecutivo *real* (quién efectivamente gobierna la provincia/nación/municipio), llevado como estado que solo cambia en años con elección ejecutiva -- nunca se deriva de `agrupacion_ganadora` de esa misma fila (eso tautologizaría `gana_oficialismo`). Para `provincial`/`nacional`, `agrupacion_ganadora` de `oficialismos.csv` es el voto *de La Plata*, que puede divergir del resultado real de la provincia/nación (divergencia real encontrada y corregida: gobernador 2019, La Plata votó JUNTOS POR EL CAMBIO pero la provincia eligió a Kicillof/FRENTE DE TODOS) -- el titular se corrige a mano en esos casos puntuales (`_TITULAR_REAL_DIVERGE_DE_VOTO_LA_PLATA`), verificado por conteo real de circuitos, no supuesto. Para `municipal` no aplica (el intendente lo elige La Plata directamente) | Adoptada |
| D13 | Panel trimestral complementario (`panel_trimestral_<nivel>.csv`, Fase 5, paralelo a `panel_ventanas.csv`, no lo reemplaza): N de trimestres por ventana es `round(meses_entre_elecciones / 3)`, variable entre 6 y 10 según la ventana real, nunca fijo en 8 -- ver corrección a D4 abajo | Adoptada |
| D14 | Panel trimestral "bielección" (`panel_bieleccion_trimestral_<nivel>.csv`, `data/tfi_data/panel/t-2/`, paralelo a D13 que pasa a vivir en `data/tfi_data/panel/t-1/`): mismo formato de fila, pero sobre el bloque largo `_vl` de `features_ventana.py` (elección t-2 a t, 4 años/dos elecciones) en vez del bloque corto `_vc`. Ventanas sin `fecha_inicio_vl` (primera transición de cada nivel, D3) se saltean -- 33 ventanas resultantes, no 36, desde D31 (antes: 28 de 31) | Adoptada |
| D15 | (cobertura de `nacional` extendida por D31; el resto de esta decisión sigue vigente) `oficialismos.csv` se extiende de 2011-2025 a 2001-2025 (`municipal`/`provincial`; en su momento `nacional` seguía sin cubrir 2001-2009, mismo criterio que `calendario_electoral.csv`/`oficialismo_por_nivel.csv`, que tampoco lo cubrían). `agrupacion_ganadora` sale de `data/tfi_data/elecciones/<año>_<nivel>.csv` (mayor cantidad de votos, excluyendo BLANCO/NULO/VOTANTES_HABILITADOS); `era_oficialismo` no se re-deriva desde cero -- reusa las mismas fuentes ya citadas en `construir_calendario.py` (`_EJECUTIVA_PRE_2011`/`_TITULAR_INICIAL_2001`, resultado de la elección ejecutiva real) y en `construir_resultado_distrito.py` (`ALIAS_LISTA_OFICIALISMO`, relabeling de listas peronistas 2005-2009). Verificado contra `data/tfi_data/resultado_distrito.csv` ya existente: `gana_oficialismo`/`share_oficialismo` de esas 10 filas, calculados hasta ahora por matching de nombre contra `oficialismo_por_nivel.csv`, coinciden exactamente con los valores de `era_oficialismo` cargados acá -- no una fuente nueva de criterio, una curación explícita de uno ya vigente. `continua_renombrada` (2007 provincial, Solá→Scioli) colapsa a `era_oficialismo=true`, mismo tratamiento binario que ya recibía cualquier renombre de frente en 2011-2025 (ej. Bruera 2007→2011, Partido Progreso Social→FPV) | Adoptada |
| D16 | Bug de matching de nombre (no hueco de adquisición) en `_resolver_oficialismo`: cuando `era_oficialismo=false` (D15), `share_oficialismo` igual necesita emparejar por nombre contra `_match_oficialismo`, y ese emparejamiento fallaba en silencio para `(2013, nacional/provincial/municipal)` -- boleta real `FRENTE PARA LA VICTORIA`, sin el prefijo "ALIANZA" que sí trae `oficialismo_por_nivel.csv` y que sí aparece en 2011/2015 (`id_agrupacion` 0501 en 2013 vs. 0131 en 2011/2015, alianza registrada distinto ese año) -- y para `(2019, nacional)` -- boleta real `JUNTOS POR EL CAMBIO`, rebranding de Cambiemos para las legislativas de 2019 (mismo `id_agrupacion` 135), mientras `oficialismo_por_nivel.csv` sigue etiquetando esa fila como `ALIANZA CAMBIEMOS`. Corregido agregando las 4 entradas a `ALIAS_LISTA_OFICIALISMO`, mismo mecanismo ya usado para 2005-2009. Auditado el resto de las 34 filas `(año,nivel)` con fila en `oficialismo_por_nivel.csv`: ningún otro caso de `era_oficialismo=false` tiene mismatch de nombre sin resolver (hay mismatches en años con `era_oficialismo=true`, ej. 2011 municipal, 2017/2019/2023/2025 varios niveles, pero esa rama no pasa por `_match_oficialismo` -- usa `ganador.share` directo -- así que no son bugs activos, no se les agrega alias). Afecta tanto `panel_trimestral_<nivel>.csv` (D13, 4 filas frontera) como `panel_bieleccion_trimestral_<nivel>.csv` (D14, 7 filas frontera -- cada año interviene dos veces en el bloque largo, una vez como `eleccion_t` y otra como `eleccion_t_menos_2` de la ventana siguiente) además de `panel_ventanas.csv` (8 `delta_v` que dependían de alguno de los 4 extremos). Verificado fila por fila (no solo conteo de nulos): `gana_oficialismo` no cambió en ninguna fila (ya lo resolvía D15 sin depender del nombre); `share_oficialismo`/`delta_v` cambiaron únicamente en las filas esperadas | Adoptada |
| D17 | `data/tfi_data/elecciones.csv` (grano `nivel`×`anio`, generado por `ml_models.construir_elecciones_resumen`) pasa a ser la fuente de verdad de la estructura de la oferta electoral (fuerzas viables, marginal, oposición principal, dispersión ideológica) y del cruce oficialismo/ausentismo que consume el panel trimestral (D13/D14) -- ver diccionario de columnas en `docs/especificaciones/especificacion_panel_temporal.md`. `resultado_distrito.csv`/`voto_partido_distrito.csv` quedan congelados: siguen en el repo y `construir_resultado_distrito.py` sigue andando si se lo corre a mano, pero ninguna otra pieza del pipeline los lee ni los regenera modificados (mismo patrón "huérfano, no eliminado" ya documentado en `CLAUDE.md` para `vparty_cuadrantes_local`). El pedido original era construir `elecciones.csv` a partir de `voto_partido_distrito.csv`; se usó en cambio `data/tfi_data/elecciones/<año>_<nivel>.csv` (detalle por partido con BLANCO/NULO/VOTANTES_HABILITADOS y V-Party ya sincronizado, ver `construir_elecciones.py`) porque `voto_partido_distrito.csv` no tiene ninguna de esas columnas y reconstruirlas ahí hubiera duplicado lógica ya resuelta y testeada. `ausentismo` usa dos fórmulas no exactamente equivalentes: años con `circuito_<cargo>.json` reusan `electores - positivos - otros_total` (`analisis.graficos._votos_no_ideologicos`), que excluye de "ausente" a categorías `RECURRIDO(S)`/`IMPUGNADO(S)`/`COMANDO`; años sin circuito (2001-2009, 2025 municipal/provincial) restan `votantes_habilitados - votos_positivos - votos_blancos - votos_nulos`, que no puede distinguir esas categorías si existieron -- el `ausentismo` de 2001-2009 podría estar levemente sobreestimado por esto, documentado como limitación, no corregido por falta de desglose real. `votos_nulos`/`ausentismo` quedan vacíos (no en 0) para `2025 provincial`/`2025 municipal`: la fuente de esos dos años (sistema separado de la Junta Electoral bonaerense, ver `docs/adquisicion_datos_especializacion.md` §1.c) no trae fila NULO. El piso de "fuerza viable" es 1.5% de `votos_positivos` (Ley 26.571, 2011) -- mismo denominador que usa `share_oficialismo` en las dos ramas de `_resolver_oficialismo` (`models.totalizar_agrupaciones` y `_voto_partido_desde_tfi`, verificado antes de escribir este módulo, no supuesto). Verificado contra los 34 `(nivel,año)` reales que ninguna elección del período fue bipartidista (entre 5 y 13 fuerzas viables siempre) -- por eso se agrega `share_otras_fuerzas_viables` (no estaba en el pedido original): sin ella, `share_oficialismo + share_oposicion_principal + share_marginal_acumulado` no se acerca a 100 (ej. 2001 provincial: 50.44 sobre 100, un 49.56 real quedaba sin ninguna columna). Con esa cuarta columna la identidad cierra por construcción: `share_marginal_acumulado + share_oposicion_principal + share_otras_fuerzas_viables + share_oficialismo (si el oficialismo es viable) = 100`; si el oficialismo no es viable (share < 1.5%), su share ya está adentro de `share_marginal_acumulado` y no se lo cuenta en `n_fuerzas_viables` (no ocurrió en los 34 casos reales del período, caso cubierto con fixture sintética en `tests/ml_models/test_construir_elecciones_resumen.py`). Dispersión ideológica ponderada por voto (μ, σ²) se calcula por separado para los ejes `vparty_economico` y `vparty_progresismo` (populismo afuera, mismo criterio que `vparty_cuadrantes_local`), solo sobre fuerzas viables y solo con score V-Party cargado (388/557 filas de `clasificacion_ideologica_agrupaciones.csv` a esta fecha) -- vacía si ninguna fuerza viable de esa elección tiene score. Las columnas nuevas se documentan en `docs/especificaciones/especificacion_panel_temporal.md`, no en `registro_variables.csv`: ese registro es para series mensuales/trimestrales externas que alimenta genéricamente `features_ventana.py` (periodicidad_nativa, es_flujo, polaridad), no para atributos estructurales por elección -- forzarlas ahí haría que `features_ventana.py` intentara calcular `_pendiente`/`_volatilidad` sobre un valor puntual por elección, sin sentido. **Extensión posterior**: se agrega `dispersion_cobertura_share` = (suma de votos de fuerzas viables con V-Party cargado) / `votos_positivos`, escala 0-100 (mismo criterio que las columnas `share_*` hermanas) -- sin esta columna, un salto en `dispersion_economico_mu`/`dispersion_progresismo_mu` entre dos elecciones no se puede distinguir de un cambio en qué fracción del voto tiene V-Party cargado; `0.0` (no vacío) si ninguna fuerza viable tiene score, a diferencia de `dispersion_economico_mu`/`sigma2`, que sí quedan vacías en ese caso -- son preguntas distintas ("cuánto sabemos" vs. "qué dice lo que sabemos") | Adoptada |
| D18 | **Auditoría de `distancia_oficialismo_alternativa`** (`panel_ventanas.csv`, calculada en `construir_resultado_distrito.calcular_distancia_oficialismo_alternativa`, invocada desde `construir_panel_ventanas.construir_panel`): mide un solo eje (`vparty_economico` -- `_construir_posiciones` nunca guarda `vparty_progresismo`), contra la fuerza con más votos entre TODAS las agrupaciones con score conocido (sin piso de viabilidad 1.5%), y matchea `of_t["agrupacion_oficialismo"]` (`oficialismo_por_nivel.csv`) **por nombre** contra la boleta real, sin pasar por el curado de `oficialismos.csv`/`ALIAS_LISTA_OFICIALISMO` que sí usa `_entrada_oficialismo` (D15/D16). Verificado contra los datos reales, no supuesto: de 31 filas de `panel_ventanas.csv`, **17 (55%) tienen la columna vacía**, y las 17 son fallo de matching de nombre, no ausencia de score V-Party (confirmado caso por caso con un script ad hoc; tras D31 el panel pasa a 36 filas y la columna deprecada queda vacía en 21 -- mismo patrón, columna que ya no se corrige) -- ej. `oficialismo_por_nivel.csv` dice `FRENTE DE TODOS` para `(2023, nacional)`/`(2023, provincial)` mientras la boleta real dice `UNION POR LA PATRIA`; dice `PARTIDO JUSTICIALISTA` en años donde la boleta real es `ALIANZA FRENTE PARA LA VICTORIA`, etc. **Resolución**: la columna queda en `panel_ventanas.csv` sin tocar, marcada DEPRECADA en el código (comentario en `construir_panel_ventanas.py`) y en la documentación -- no se corrige in-place (sería reimplementar lo que la tabla nueva ya resuelve de raíz) ni se elimina (principio aditivo del repo). Sucedida por **`data/tfi_data/distancias_ideologicas.csv`** (`ml_models.construir_distancias_ideologicas`, grano `nivel×anio×agrupación`, una fila por fuerza viable de cada elección -- no una sola "alternativa" preseleccionada), que resuelve la identidad del oficialismo por **identidad de objeto** contra `_entrada_oficialismo` en vez de por nombre (verificado con los mismos casos reales 2023 nacional/provincial: la fila `es_oficialismo=True` resulta `UNION POR LA PATRIA`, no `FRENTE DE TODOS`, en `tests/ml_models/test_construir_distancias_ideologicas.py::TestCasoRealAuditoria2023`). Distancias por eje (`distancia_economico_al_oficialismo`, `distancia_progresismo_al_oficialismo`) guardadas **con signo** (`score_fuerza − score_oficialismo`, no valor absoluto) -- son las componentes de `distancia_euclidea_al_oficialismo = √(economico² + progresismo²)`, y el signo indica de qué lado del oficialismo cae cada alternativa (relevante para H2/H3). Fila del propio oficialismo: distancias `0.0` siempre, aunque no tenga score propio cargado (distancia de un punto a sí mismo). Si el oficialismo no tiene score V-Party: ninguna distancia de esa elección se puede calcular (sin referencia) -- no ocurrió en los 34 casos reales. `panel_ventanas.csv` suma además `delta_dispersion_economico_mu`/`delta_dispersion_progresismo_mu` (`mu(t) − mu(t−1)` por eje, join contra `elecciones.csv`), `magnitud_desplazamiento_ideologico` (`√(delta_economico² + delta_progresismo²)`) y `cuadrante_desplazamiento` -- criterio de cuadrante: mismo signo que las etiquetas fijas de `analisis.vparty_cuadrantes`/`vparty_cuadrantes_local` (positivo en económico → derecha, positivo en progresismo → progresista), aplicado al **vector de desplazamiento** en vez de al punto absoluto (no existe una función reusable de cuadrantes en esos módulos, solo dibujan las 4 etiquetas como texto fijo); `None` si falta cualquiera de los dos deltas o si alguno es exactamente `0` (cuadrante indefinido, no se fuerza un lado arbitrario). `dispersion_cobertura_share_min` = `min` de la cobertura V-Party en las dos puntas de la transición, agregada para que el notebook de H2/H3 pueda ponderar/filtrar por baja cobertura sin fijar un umbral en código. **Disciplina H1/H4 vs. H2/H3**: estas 4 columnas de desplazamiento son variables dependientes de H2 calculadas con datos de `t` -- nunca predictoras de H1/H4. Confirmado que no hace falta tocar código de `notebooks/ml/ventana_t-1/01.1_lasso_voto_valido.ipynb`/`02_bayes.ipynb` para garantizarlo: `01.1_lasso_voto_valido.ipynb` selecciona features por sufijo (`c.endswith("_vc")`), `02_bayes.ipynb` usa una única variable hardcodeada (`icg_pendiente_vc`) -- ninguna de las 4 columnas nuevas termina en `_vc`/`_vl`, quedan excluidas por construcción. Advertencia dejada en el notebook nuevo (`notebooks/ml/ventana_t-1/03_desplazamiento_ideologico.ipynb`): si algún día `01.1_lasso_voto_valido.ipynb` cambia a "todas las columnas menos una blacklist", esas 4 columnas deben agregarse a mano a esa blacklist. **Extensión
posterior**: se agregan `delta_sigma2_economico`/`delta_sigma2_progresismo`
(`dispersion_<eje>_sigma2(t) - dispersion_<eje>_sigma2(t-1)`, mismo join y
misma función `calcular_delta_dispersion` que `delta_dispersion_*_mu`,
ahora con un parámetro `estadistico` -- `"mu"` por default, sin cambiar
comportamiento de los 2 usos existentes). `delta_dispersion_*_mu` mide
hacia dónde se mueve el **centro** de la dispersión ideológica del
electorado y puede cancelarse cuando fuerzas de polos opuestos ganan
votos parecidos entre `t-1` y `t`; `delta_sigma2_*` mide la **varianza**
(fragmentación/polarización) y no cancela ese caso -- son dos preguntas
distintas del mismo fenómeno, ambas necesarias. Caso real que motiva la
distinción (nacional, 2019→2021): `delta_dispersion_economico_mu` = +0.20
(corrimiento moderado del centro) vs. `delta_sigma2_economico` = +0.48
(la varianza creció proporcionalmente más que el centro se movió) --
consistente con que 2021 sumó crecimiento simultáneo de fuerzas en polos
opuestos (LLA a la derecha, FIT-U a la izquierda), un desplazamiento que
`mu` por sí solo subestima. Mismo criterio de exclusión de H1/H4 que las
4 columnas anteriores: son variables dependientes de H2/H3 (fragmentación
en `t`), y por el mismo mecanismo de selección de features (sufijo `_vc`/`_vl`
en `01.1_lasso_voto_valido.ipynb`, variable única hardcodeada en `02_bayes.ipynb`)
quedan excluidas por construcción, sin tocar esos dos notebooks. Además,
`data/tfi_data/distancias_ideologicas.csv` gana una pestaña interactiva
propia (`src/visualizacion/distancia_por_fuerza.py`, ver
`docs/FUNCIONALIDADES.md`) -- distancia euclídea al oficialismo con signo
(derecha/izquierda según el eje económico) por fuerza viable, año y
nivel; paleta azul/rojo/gris (derecha/izquierda/oficialismo-o-empate)
propia de esa pestaña (no sale de los CSV de
colorimetría, que son para campo ideológico/filiación política absolutos,
no para una distancia relativa al oficialismo) | Adoptada |
| D22 | **Redundancia algebraica en el bloque de voto exit de `panel_ventanas.csv` (D19)**: `voto_exit_total_pct_t`/`_t_menos_1` (= `voto_exit_ausentismo_pct_t + voto_exit_blanco_nulo_pct_t`, por punta) y `delta_voto_exit_total_pct` (= diferencia de los anteriores) se retiran del panel -- verificado de forma independiente sobre las 31 filas reales: 62/62 puntas `t`/`t_menos_1` cumplen la identidad de suma con `0.0` de diferencia exacta (sin tolerancia de punto flotante) y las 31 filas cumplen la identidad de delta también con `0.0` exacto, sin ningún faltante. Estas 3 columnas no aportaban información que no estuviera ya en `voto_exit_ausentismo_pct_t`/`_t_menos_1` + `voto_exit_blanco_nulo_pct_t`/`_t_menos_1`. Se detectó además una asimetría real: `delta_participacion_pct` sí tenía delta propio pero ausentismo y blanco/nulo por separado no, pese a ser dos fenómenos políticamente distintos (no ir a votar vs. participar sin adhesión partidaria) y ambos candidatos legítimos a modelarse por separado -- se agregan `delta_voto_exit_ausentismo_pct`/`delta_voto_exit_blanco_nulo_pct` (`ml_models.construir_elecciones_resumen.calcular_delta_voto_exit_ausentismo`/`calcular_delta_voto_exit_blanco_nulo`, mismo esqueleto que `calcular_delta_participacion`, en el lugar donde vivía la ahora eliminada `calcular_delta_voto_exit_total`). `ParticipacionVotoExit`/`calcular_participacion_voto_exit` (`construir_elecciones_resumen.py`) **no se tocan**: `voto_exit_total_pct` sigue existiendo como cantidad intermedia calculada ahí, solo deja de materializarse como columna del panel. **Excepción puntual y documentada a la política de cambios aditivos del repo** (que en general prohíbe eliminar/renombrar columnas de un CSV trackeado): se justifica porque son columnas comprobadamente redundantes, no una reestructuración -- mismo espíritu que la Corrección a D6 (que también retiró una columna trackeada, `periodo_intervenido`, aunque ahí por falta de lectores y acá por redundancia algebraica exacta). Impacto real encontrado y resuelto: `notebooks/ml/ventana_t-1/01.3_lasso_voto_exit.ipynb` usaba `delta_voto_exit_total_pct` como variable dependiente en 3 celdas (ajuste LASSO, LOO-CV, chequeo de estabilidad) -- se agregó una celda que reconstruye esa columna localmente en el notebook como `delta_voto_exit_ausentismo_pct + delta_voto_exit_blanco_nulo_pct`, sin reintroducir la columna redundante en `panel_ventanas.csv`. `panel_trimestral_<nivel>.csv`/`panel_bieleccion_trimestral_<nivel>.csv` no se tocan (no leen `panel_ventanas.csv`) | Adoptada |
| D23 | **Metadata de `panel_ventanas.csv` nunca como regresor**: `id_transicion`, `fecha_inicio_vc`, `fecha_fin_vc`, `fecha_inicio_vl`, `anio_t`, `anio_t_menos_1`, `anio_t_menos_2` identifican qué ventana produjo cada fila (trazabilidad/auditoría), no son variables explicativas -- año/fecha cruda actuaría como proxy de tendencia temporal con N=7-12 por nivel si entrara a un modelo. Auditoría (no asumida, recalculada): `src/ml_models/cargar_panel.py` no filtra columnas (solo filas, por `nivel`); el único punto real de armado de `X` en todo el repo es `ml_models.lasso.construir_Xy_final`/`estabilidad_seleccion`, alimentado por listas de columnas armadas en los notebooks. Las 4 notebooks que arman esa lista por sufijo (`01.1`, `01.2`, `01.3`, `03_desplazamiento_ideologico.ipynb`) usaban `c.endswith("_vc") and pd.api.types.is_numeric_dtype(df[c])` -- verificado que hoy excluye a las 7 columnas, pero por dos motivos distintos: `id_transicion`/`fecha_inicio_vl`/`anio_t`/`anio_t_menos_1`/`anio_t_menos_2` fallan el sufijo (`anio_*` son numéricas, pasarían un filtro que fuera solo "todas las numéricas"); `fecha_inicio_vc`/`fecha_fin_vc` sí terminan en `_vc` igual que las variables económicas y solo quedaban afuera porque `pd.read_csv` las tipa como string -- exclusión accidental, no por diseño. `02_bayes.ipynb`/`02_bayes_2.ipynb` usan selección hardcodeada de una sola variable, sin este riesgo. No existe (todavía) ningún modelo RF/GB de contraste en el repo -- es el "próximo modelo" contra el que se blinda esta decisión. **Resolución**: `COLUMNAS_METADATA_PANEL` (tupla con las 7, `src/ml_models/cargar_panel.py`) como única fuente de verdad, con dos capas de uso: `columnas_candidatas(df, sufijo="_vc")` reemplaza el filtro ad hoc duplicado en las 4 notebooks (mismo resultado hoy, ahora explícito); `construir_Xy_final`/`estabilidad_seleccion` (`ml_models/lasso.py`) levantan `ValueError` si alguna columna de metadata aparece en la lista `columnas` recibida, protegiendo también a un modelo futuro que arme su lista de otra forma (p.ej. `.select_dtypes` a lo bruto) pero reuse esas dos funciones para no reimplementar el manejo de NaN. Auditado por separado (sin hallazgos, sin tocar código): `gana_oficialismo`/`continuidad_oficialismo` no aparecen en ningún notebook de modelado; `delta_v` aparece siempre y solo como variable dependiente | Adoptada |
| D24 | **`resultado_disponible` retirada de `panel_ventanas.csv`**: no es "resultado electoral faltante" (`delta_v`/`gana_oficialismo`/`share_oficialismo` están completos en las 12 filas `False` de 31) sino un **flag de procedencia de datos** -- `True` si la fila salió del pipeline de `circuito_<cargo>.json` (API Ministerio del Interior, 2011+), `False` si salió de una fuente alternativa (2001-2009 municipal/provincial, sin circuito pre-2011, D15; 2025 provincial/municipal, sistema separado de la Junta Electoral bonaerense, Ley 5.109, `docs/adquisicion_datos_especializacion.md` §1.c). Se calcula dos veces por separado con la misma función (`_circuito_disponible`, `construir_resultado_distrito.py:38-39`): en `elecciones.csv` (`construir_elecciones_resumen.py`, vigente) y en `resultado_distrito.csv` (`construir_resultado_distrito.py`) -- `panel_ventanas.csv` arma la suya como AND de ambas puntas de la transición leyendo de la **segunda**, no de `elecciones.csv` (`construir_panel_ventanas.py`, vía `resultado_por_anio_nivel`). **Hallazgo adyacente, señalado sin corregir**: esto contradice la afirmación de `CLAUDE.md`/especificación de que `resultado_distrito.csv` está "congelado... ninguna otra pieza del pipeline lo lee" -- `construir_panel_ventanas.py` lo sigue leyendo en vivo, no solo para `resultado_disponible` sino también para `gana_oficialismo`/`share_oficialismo`/`delta_v` de `panel_ventanas.csv`. Verificado (no bug activo): `resultado_distrito.csv` es un día más viejo que `elecciones.csv` (no incluye la corrección de ausentismo de D17) pero las 14 filas `(nivel,año)` relevantes coinciden exactamente entre ambos archivos en `resultado_disponible`/`gana_oficialismo`/`share_oficialismo` -- sin divergencia real hoy. **Auditoría de uso** (`src/`, `tests/`, los 7 notebooks de `notebooks/ml/`, `docs/`): ninguna referencia es crítica -- nunca filtra filas, nunca condiciona qué entra a un modelo o a otro cálculo (la variable local `disponible`, previa al nombre del campo, sí decide qué fórmula de `ausentismo`/`participacion` usar, pero produce una fila igual en cualquier rama, y nada relee la columna ya escrita para decidir algo después). **Alcance de la eliminación, acotado explícitamente**: solo `panel_ventanas.csv` (`construir_panel_ventanas.py`, se saca la asignación `fila["resultado_disponible"]` y la entrada en `columnas_id`, sin tocar `resultado_t`/`resultado_t1`, que se siguen usando para `delta_v`/`gana_oficialismo`/`share_oficialismo`) -- **no** se toca `elecciones.csv`/`construir_elecciones_resumen.py` (conserva su propio `resultado_disponible`, correcto, por `(nivel,año)`) ni `COLUMNAS_ELECCION_PANEL`, así que `panel_trimestral_<nivel>.csv`/`panel_bieleccion_trimestral_<nivel>.csv` (que lo heredan de `elecciones.csv`) quedan intactos, sin regenerar. Tampoco se toca `construir_resultado_distrito.py`/`resultado_distrito.csv` (congelado): solo se deja de leer ese campo hacia `panel_ventanas.csv`, `FilaResultadoDistrito` conserva el campo. Ningún test necesitó cambios (ninguno leía `fila["resultado_disponible"]`, la clave del dict de salida del panel) | Adoptada |
| D25 | **`X_final_vl` y `X_mejoro` retiradas de `panel_ventanas.csv` (9 + 8 variables macro)**: dos redundancias estructurales confirmadas de forma independiente sobre los datos reales (0 excepciones en ambos casos, 222 comparaciones cada una). `X_final_vl == X_final_vc` siempre: `vc` y `vl` comparten literalmente `fecha_fin_vc` como fecha de fin (`ventanas.csv` no tiene columna `fecha_fin_vl`), así que `_final()` (promedio de los últimos 6 meses de la ventana) da el mismo resultado en ambas -- para cualquier variable, no una casualidad de los datos actuales. `X_mejoro == signo(X_delta_nivel, polaridad)` siempre: es una función determinística de `delta_nivel` (que se queda) y `polaridad` (columna de `registro_variables.csv`, que también se queda) -- ídem, vale para cualquier variable. **Diagnóstico de por qué `resultado_fiscal` no tenía `_mejoro`**: intencional (`polaridad=ambigua` en el registro, y `calcular_features_interventana_variable` nunca agregaba la clave para esa polaridad), pero la razón puntual de por qué esa variable es `ambigua` (a diferencia de por qué `es_flujo=false`, que sí está explicada en su `nota_metodologica`) no estaba escrita en ningún lado -- señalado, no se corrige ni se agrega `resultado_fiscal_mejoro` (fuera de alcance). **Auditoría de uso** (`src/`, `tests/`, los 7 notebooks, `docs/`): cero crítico en ambos casos -- `cargar_panel.columnas_candidatas` (sufijo `_vc` únicamente) ya excluía `_final_vl` estructuralmente de todo notebook, y excluía `_mejoro` por partida doble (no termina en `_vc`, y carga como `dtype=object` en el CSV real por los `NaN` de la primera ventana de cada nivel). **Mecanismo de eliminación, de raíz en `features_ventana.py`** (no post-hoc en `construir_panel_ventanas.py`): ninguna variable, actual o futura, vuelve a generar estas columnas -- ambas redundancias son estructurales (dependen del esquema de `ventanas.csv` y de la relación `delta_nivel`+`polaridad`, no de qué variable es), así que atacarlas en el motor genérico evita que reaparezcan solas si se agrega una variable nueva al registro (D9). Confirmado que `calcular_features_ventana_variable`/`calcular_features_interventana_variable` no las usa nada más que `construir_panel_ventanas.py` -- `construir_panel_trimestral.py`/`construir_panel_bieleccion_trimestral.py`/`trayectorias_economicas*.py` importan de `features_ventana.py` solo `_meses_en_ventana`, sin relación, así que quedan intactos. `polaridad` como columna de `registro_variables.csv` **se queda** (sigue documentando la dirección esperada de cada variable, útil para interpretar el signo de un coeficiente de modelo, aunque ya no alimente una columna derivada). Test de extensibilidad (D9, `test_panel_ventanas.py::TestExtensibilidad`) actualizado para esperar que una variable nueva NO traiga `_mejoro` (ya no se genera para ninguna polaridad) | Adoptada |
| D26 | **Atributos de ventana para las 6 variables EPH en `panel_ventanas.csv`**: se extiende `features_ventana.py` para calcularles el mismo tipo de features de trayectoria que ya tienen las 9 variables macro -- paquete completo (10 columnas: `_cobertura_parcial`, `_delta_nivel`, `_delta_pendiente`, `_final_vc`, `_nivel_vc`, `_nivel_vl`, `_pendiente_vc`, `_pendiente_vl`, `_volatilidad_vc`, `_volatilidad_vl`) para `tasa_informalidad` (única de las 6 con comportamiento cíclico esperable, comparable a `desocupacion`), paquete reducido (3 columnas: `_cobertura_parcial`, `_delta_nivel`, `_nivel_vc`) para `pct_sin_cobertura_salud`/`hacinamiento_medio`/`pct_hogares_ayuda_social_gobierno`/`pct_hogares_prestamo_bancario`/`pct_hogares_vendio_pertenencias` -- series de ritmo más lento/estructural (encuesta de hogares, quiebres trimestrales más ruidosos que informativos) que no necesitan el detalle de trayectoria completo en una ventana de 24 meses. (Nota: la decisión de incorporar las 6 variables EPH al registro es **D21**, no D17 como se citó en el pedido original de esta tarea -- D17 es sobre la estructura de `elecciones.csv`, sin relación). **Mecanismo**: nueva columna `paquete_atributos` (`completo`/`reducido`) en `registro_variables.csv` y campo homónimo en `FilaRegistroVariable` (`cargar_series_economicas.py`, default `"completo"` a nivel de dataclase por conveniencia de tests, pero cada fila real del registro lleva su valor explícito) -- se prefirió sobre una lista explícita de sufijos por variable (alternativa también evaluada) por ser la generalización mínima que resuelve el caso real, sin diseñar para un paquete hipotético que todavía no existe. `calcular_features_ventana_variable`/`calcular_features_interventana_variable` (`features_ventana.py`) validan el valor (`_PAQUETES_VALIDOS`, mismo patrón que `_POLARIDADES_VALIDAS`) y, si es `reducido`, ni arman la ventana `vl` (aunque `fecha_inicio_vl` exista) ni calculan `_pendiente`/`_volatilidad`/`_final`/`_acum`/`_delta_pendiente` -- las claves correspondientes ni se agregan al diccionario de resultado, así que esas columnas no aparecen en absoluto en `panel_ventanas.csv` para esas 5 variables (no quedan vacías). Las 9 variables macro no cambian de comportamiento (`paquete_atributos=completo` reproduce el camino de código preexistente). Verificado por simulación sobre los datos reales (`series_economicas_mensuales.csv`/`ventanas.csv`) antes de escribir nada: 10 + 5×3 = 25 columnas nuevas exactas, `panel_ventanas.csv` pasa de 126 a 151 columnas; `cobertura_parcial` refleja correctamente los huecos EPH ya documentados (2007T3, 2015T3, 2015T4, 2016T1) sin ningún cambio de convención, heredado del mismo chequeo `len(valores) < len(meses)` que ya usan las 9 macro (verificado con transiciones reales que rozan esos huecos, ej. `municipal_2013_2015`/`municipal_2015_2017`, `cobertura_parcial=True` en ambas). `panel_trimestral_<nivel>.csv`/`panel_bieleccion_trimestral_<nivel>.csv` no se tocan (no usan `calcular_features_ventana_variable`/`calcular_features_interventana_variable`). Consecuencia downstream señalada, no corregida: `cargar_panel.columnas_candidatas` (sufijo `_vc`) va a recoger automáticamente las columnas `_vc` de estas 6 variables como candidatas a feature la próxima vez que se corra algún notebook de `notebooks/ml/` -- comportamiento correcto y esperado (D9), no una fuga | Adoptada |
| D27 | **Se reemplaza el filtro por sufijo `_vc` de `columnas_candidatas()` por exclusión explícita de metadata + outcome electoral** -- exactamente la fuga que D26 dejó señalada: el sufijo excluía sin querer `X_cobertura_parcial`/`X_delta_nivel`/`X_delta_pendiente`/`X_nivel_vl`/`X_pendiente_vl`/`X_volatilidad_vl`/`X_acum_vl` de las 15 variables (macro + EPH), que sí deben poder ser features. Auditoría previa a tocar nada: el sufijo también actuaba, sin que nadie lo haya diseñado así, como protección accidental contra fuga de variable dependiente -- ninguna columna de resultado electoral termina en `_vc`. Se verificó con pandas (no asumido) que `gana_oficialismo`/`participacion_relevante_t`/las 15 `X_cobertura_parcial` son `dtype=bool` (`is_numeric_dtype=True`, pasarían un filtro "todas las numéricas" sin problema) y que `cuadrante_desplazamiento`/`agrupacion_oficialismo`/`continuidad_oficialismo`/`nivel`/`tipo_eleccion_t*` son `str` (se salvan por dtype, no por diseño -- mismo patrón fragil que `fecha_inicio_vc`/`fecha_fin_vc` en D23). Se armó una lista exhaustiva de 24 columnas outcome-o-derivadas-de-outcome (`COLUMNAS_OUTCOME_ELECTORAL`, `cargar_panel.py`, junto a `COLUMNAS_METADATA_PANEL`), verificada contra `construir_panel_ventanas.py`/`construir_elecciones_resumen.py` línea por línea, más allá de la lista semilla del pedido (agrega el bloque completo de participación/voto-exit, D19/D22, y `dispersion_cobertura_share_min`, D18) -- cubre simétricamente la familia propia de cada uno de los dos objetivos de modelado que conviven en `panel_ventanas.csv` (Objetivo 1: `delta_v`/`delta_participacion_pct`/`delta_voto_exit_total_pct` en `01.1`/`01.2`/`01.3`; Objetivo 2: `magnitud_desplazamiento_ideologico` en `03_desplazamiento_ideologico.ipynb`) para que el target de uno no se cuele como feature del otro. `columnas_candidatas(df, sufijo="_vc")` (D23) pasa a `columnas_candidatas(df, excluir_adicional=())`: todas las columnas numéricas del panel, excluyendo `COLUMNAS_METADATA_PANEL` ∪ `COLUMNAS_OUTCOME_ELECTORAL` ∪ `excluir_adicional` -- se saca el parámetro `sufijo` (confirmado que ningún llamador lo usaba con un valor distinto del default). Cada uno de los 4 notebooks pasa además su propio target en `excluir_adicional` como defensa en profundidad -- para 4 de los 5 ya es redundante con la lista compartida, pero es la única protección real para `01.3_lasso_voto_exit.ipynb`, cuyo target (`delta_voto_exit_total_pct`) se construye en el notebook sumando `delta_voto_exit_ausentismo_pct` + `delta_voto_exit_blanco_nulo_pct` y no es columna real del panel (D22) -- sin esa exclusión explícita, esos dos componentes quedarían perfectamente colineales con el target. Se extendió además la guarda defensiva `_rechazar_metadata` de `lasso.py` (D23) para rechazar también `COLUMNAS_OUTCOME_ELECTORAL`, protegiendo a `01.1`/`01.2`/`01.3` (que importan `construir_Xy_final`/`estabilidad_seleccion`) aunque el notebook arme su lista de candidatas de otra forma en el futuro -- no protege a `03_desplazamiento_ideologico.ipynb`, que tiene su propia copia local de esas funciones (mismo límite que D23 ya señalaba). No se tocó `encontrar_redundantes`/`elegir_representante`/`columnas_nan`/`estandarizar`: operan sobre lo que reciben, sin lógica propia sobre qué columnas son válidas. **Simulación sobre datos reales antes de escribir nada, y verificación end-to-end después con el código ya modificado**: los 5 objetivos pasan de 46 a 117 columnas candidatas (0 columnas perdidas, +71 nuevas -- las 15 `cobertura_parcial` marcadas explícitamente como features legítimas de calidad de dato, no de outcome; 15 `delta_nivel`; y `delta_pendiente`/`nivel_vl`/`pendiente_vl`/`volatilidad_vl`/`acum_vl` para las 10 variables de paquete completo, D26), confirmado con el código real que ninguna de las 24 columnas de `COLUMNAS_OUTCOME_ELECTORAL` (ni, para `01.3`, sus dos componentes) termina en ningún `X` final de los 5 objetivos | Adoptada |
| D28 | **`construir_Xy_final` (`lasso.py` + copia local de `03_desplazamiento_ideologico.ipynb`) descarta columnas sin varianza y castea a `float` tras el filtro de NaN, antes de estandarizar** -- dos bugs latentes que D27 (ampliar candidatas de 46 a 117) expuso al correr `01.1_lasso_voto_valido.ipynb` por primera vez con el universo nuevo, ninguno de los dos activo con el universo viejo. (1) `ZeroDivisionError` en `estandarizar()`: `icc_cobertura_parcial`/`icg_cobertura_parcial`/`tc_oficial_cobertura_parcial` son constantes (`False`) en las filas que sobreviven el filtro de NaN para un nivel dado -- una variable bien cubierta puede tener su propio flag de cobertura sin varianza real una vez que se restringe a las filas completas de ese nivel, aunque sí varíe sobre el panel entero. Resuelto agregando, después del filtro de NaN existente, un descarte de columnas con `std(ddof=0) == 0` (con mensaje `[nivel] excluye columna(s) sin varianza: [...]`, mismo estilo que el mensaje de NaN ya existente). (2) `UFuncTypeError` (dtype `object`) en `np.linalg.lstsq`: una vez sacadas las 3 columnas constantes, las `cobertura_parcial` restantes (con varianza real) son `dtype=bool` y, mezcladas con el resto `float64` del DataFrame, hacen que `X.values`/`X_std` completo sea `dtype=object` -- resuelto con `X_final = X_final.astype(float)` al final (codificación estándar `True`/`False` -> `1.0`/`0.0`). Ambos fixes confirmados con un script de diagnóstico standalone antes de tocar código (no a prueba y error) y aplicados de forma idéntica en las 2 copias de la función (`lasso.py`, y la local de `03_desplazamiento_ideologico.ipynb` -- mismo límite de duplicación ya señalado en D23/D27 para notebooks que no importan `lasso.py`) | Adoptada |
| D29 | **Se saca el filtro manual `startswith("emae_")`/`startswith("resultado_fiscal_final_vc")` que sobrevivía en las 4 notebooks pese a D27, y se corrige un bug de varianza-cero por fold que ese filtro venía tapando**. Auditoría: las 4 notebooks (`01.1`/`01.2`/`01.3`, `03_desplazamiento_ideologico.ipynb`) tenían, después de armar `columnas_finales`/`columnas_finales_por_nivel` por colapso de clusters (D27), una línea adicional que sacaba a mano toda columna `emae_*` (y, en `01.1`/`01.2`/`01.3`, además `resultado_fiscal_final_vc` para nacional) -- remanente de antes de que `construir_Xy_final` descartara filas con NaN automáticamente (estas columnas rompían `municipal_2001_2003`/`provincial_2001_2003`/`nacional_2013_2015` antes de esa protección). Confirmado con el pipeline real, antes de sacar nada: esas filas ya se excluyen igual por NaN en **otras** columnas (`desocupacion_delta_nivel`, `icc_delta_nivel`, etc., ver el propio diagnóstico de `01.1` en `columnas_nan`), así que el filtro de `emae_*` ya no evitaba ninguna fila perdida, solo tapaba esas variables de la evaluación de coeficientes; y `resultado_fiscal_final_vc` nunca sobrevive el colapso de clusters de todos modos (D27, siempre cae en el mismo cluster que `resultado_fiscal_nivel_vc`) -- el filtro era un no-op. Se sacaron ambas líneas en las 4 notebooks (y, donde quedaban como recómputo idéntico de `datos_final` sin el filtro, la celda duplicada completa, para no dejar código muerto). **Bug nuevo expuesto al correr las 5 con `emae_*` ya como candidata real**: `emae_cobertura_parcial` tiene varianza real sobre el panel completo de un nivel (sobrevive el filtro de D28) pero se vuelve constante dentro de un fold de LOO-CV específico si la única fila que aporta esa varianza es justo la dejada afuera -- `estandarizar()` dividía por ese desvío-cero, y como el numerador también da cero en ese caso (la fila con el valor distinto es la que está afuera del fold), el resultado correcto es `0`, pero la división `0/0` da `NaN` en punto flotante, contaminando todo `mean_mse` de esa corrida de LOO-CV y produciendo un `ValueError: zero-size array to reduction operation maximum` al filtrar `alphas[mean_mse <= umbral]` (las 4 notebooks que corrieron con `emae_*` habilitada lo reprodujeron: `01.1_lasso_voto_valido.ipynb` casualmente no, por una combinación de columnas distinta tras el colapso de clusters municipal-only que ese notebook reusa). **Resolución, en `estandarizar()` (`lasso.py` + copia local de `03_desplazamiento_ideologico.ipynb`)**: `desvios_seguros = np.where(desvios == 0, 1.0, desvios)` antes de dividir, y se devuelve ese array (no el original) como tercer elemento de la tupla -- como el numerador ya es exactamente `0` cuando el desvío de entrenamiento es `0`, el resultado da `0` en vez de `NaN`; para el punto de test estandarizado con esos mismos `desvios_tr`, el coeficiente de esa columna en el modelo ajustado es necesariamente `0` (coordinate descent nunca activa una columna con varianza cero en el training, `rho_j=0` siempre), así que el valor que tome su versión estandarizada del test no afecta la predicción final. Verificado con un script de diagnóstico que reprodujo el `ValueError` antes del fix y confirmó su desaparición después (mismos `alpha_min`/`alpha_1se` para municipal/provincial, que no tocaban el bug; nacional cambia de 11.14 a 10.22 porque antes el bug corrompía ese cálculo en silencio para casos donde sí alcanzaba a producir un resultado espurio en vez de reventar). `pytest` sin cambios (619 passed) -- el bug y el fix viven enteramente en el pipeline de modelado, no en `src/ml_models/` fuera de `lasso.py`. Las 4 notebooks se re-ejecutaron de punta a punta tras el fix; hallazgo sustantivo nuevo: `emae_pendiente_vc` sobrevive con coeficiente estable (positivo, ~2-3) en `01.3_lasso_voto_exit.ipynb` a nivel nacional bajo `alpha_1se`, incluyendo el chequeo de estabilidad leave-one-transition-out (5/6 corridas) -- primera señal de EMAE en el ejercicio de LASSO, no evaluable antes de esta tarea | Adoptada |
| D21 | **Se incorporan 6 variables de la EPH (aglomerado Gran La Plata) a `registro_variables.csv`**: `tasa_informalidad`, `pct_sin_cobertura_salud`, `hacinamiento_medio`, `pct_hogares_ayuda_social_gobierno`, `pct_hogares_prestamo_bancario`, `pct_hogares_vendio_pertenencias` -- y, por extensión genérica (D9), a `panel_trimestral_<nivel>.csv`/`panel_bieleccion_trimestral_<nivel>.csv` (Fase 5). Primera vez que `nivel_geografico=aglomerado` aparece en ese registro -- las 9 variables previas son todas `nacional`. Se declaran `periodicidad_nativa=trimestral`, mismo tratamiento que `resultado_fiscal`: `cargar_series_economicas._homogeneizar_mensual` repite el valor calendario dentro de su propio trimestre (`_LOOKBACK_MESES["trimestral"]=3` meses) y nunca hacia un trimestre sin dato propio -- no hizo falta ningún criterio de encaje nuevo ni tocar `construir_panel_trimestral.py`/`construir_panel_bieleccion_trimestral.py` (D9: agregar una variable no requiere refactorizar). Los huecos reales de la fuente (2007T3, 2015T3, 2015T4, 2016T1, INDEC no publicó) se propagan como celda vacía, igual que cualquier otro hueco del registro -- no se reintroduce una columna de flag tipo `periodo_intervenido` (retirada en la Corrección a D6 por falta de lectores); el criterio documental sigue siendo `nota_metodologica` por variable. La fuente es el aglomerado Gran La Plata (INDEC, código 2: incluye los partidos de La Plata, Berisso y Ensenada), no el Partido de La Plata exclusivamente -- mismo desajuste geográfico ya aceptado para `desocupacion` (ahí nacional). `tasa_informalidad` se calcula solo sobre asalariados con `PP07H` válido (excluye patrón/cuentapropia del denominador, no solo del numerador, ver `src/socioeconomia/eph_client.py`). `polaridad=negativa` y `estado=complementaria` para las 6 (mismo estado que `emae`; `pct_hogares_ayuda_social_gobierno`/`pct_hogares_prestamo_bancario` se leen como proxy de estrés económico del hogar, no de política/inclusión financiera, siguiendo el marco ya documentado en `data/socioeconomia/SISTEMATIZACION_VARIABLES.md`). **No se modifica** `panel_ventanas.csv`/`features_ventana.py` en esta ronda: ambos leen genéricamente `registro_variables.csv`/`series_economicas_mensuales.csv` (D9) y en teoría absorberían estas 6 variables la próxima vez que se regeneren, pero ese cálculo de atributos de trayectoria (nivel/pendiente/volatilidad) sobre estas variables queda deliberadamente para una tarea aparte | Adoptada |
| D19 | **Participación y voto exit unificado (blanco+nulo)**: las elecciones bonaerenses 2025 (provincial y municipal, desdobladas del calendario nacional) corrieron bajo la Ley provincial 5.109, que define solo tres categorías de voto -- afirmativo, en blanco, de identidad impugnada -- y **elimina la categoría "nulo"** (lo antes clasificado nulo pasa a computarse como blanco, Art. 88; fuente: Infobae, 02/09/2025, "Voto nulo y en blanco: qué diferencia habrá en las elecciones de Buenos Aires 2025"). Por eso `votos_nulos` falta específicamente en `elecciones.csv` para 2025 provincial/municipal -- no es un hueco de adquisición, es un cambio de régimen legal real (ver también la corrección a D17 de esta misma fecha, que corrige un bug real que además dejaba `ausentismo` vacío para esos dos años sin necesidad). **Decisión**: blanco y nulo se tratan como **una sola categoría unificada** (`votos_blancos_y_nulos`, `elecciones.csv`, corrección a D17) en las cinco elecciones del período, no como dos variables separadas -- para no introducir ruido comparando una distinción que solo existe bajo un régimen legal y no bajo el otro. Sobre esa columna ya resuelta en origen, `ml_models.construir_elecciones_resumen.calcular_participacion_voto_exit` arma, por `(nivel, año)`: `participacion_pct` = `(votos_positivos + votos_blancos_y_nulos) / votantes_habilitados * 100` (pass-through de `elecciones.csv`); `voto_exit_ausentismo_pct` = `ausentismo / votantes_habilitados * 100`; `voto_exit_blanco_nulo_pct` = `votos_blancos_y_nulos / votantes_habilitados * 100`; `voto_exit_total_pct` = suma de los dos anteriores (= `100 - participacion_pct`); `participacion_relevante` = `participacion_pct > 79.0` (`PARTICIPACION_BENCHMARK_NACIONAL_PCT`). Ese 79% es un **benchmark externo**, no propio del distrito: promedio nacional 1983-2023 (presidenciales + legislativas, todo el país), según Dirección Nacional Electoral/Ministerio del Interior, citado en Chequeado (26/10/2025, "la participación electoral de este domingo fue la más baja desde 1983"). La participación real de La Plata 2001-2025 (34 elecciones, 3 niveles) da **75.7%**, por debajo del benchmark -- no comparable 1 a 1: otra población (nacional, todo el país, vs. un distrito único) y otro período (1983-2023 vs. 2001-2025). `panel_ventanas.csv` agrega, por transición, `participacion_pct_t`/`_t_menos_1`, `participacion_relevante_t`, `voto_exit_ausentismo_pct_t`/`_t_menos_1`, `voto_exit_blanco_nulo_pct_t`/`_t_menos_1`, `voto_exit_total_pct_t`/`_t_menos_1` y `delta_participacion_pct`/`delta_voto_exit_total_pct` (`t - t_menos_1`, mismo esqueleto `.get()` doble + `None` si falta cualquiera que `calcular_delta_dispersion`, D18). Gracias a la corrección a D17, `voto_exit_ausentismo_pct`/`voto_exit_total_pct` de las transiciones que tocan 2025 **ya no son `None`** -- salen de `ausentismo` correcto en `elecciones.csv`, no de un fallback ad hoc en el panel (verificado con las 3 transiciones reales `*_2023_2025`: `voto_exit_total_pct_t` da 38.26/38.43/31.64 para municipal/provincial/nacional respectivamente, ninguna vacía). | Adoptada |
| D20 | **Validación del oficialismo inicial en la ventana 2001-2003 y retiro de `sin_oficialismo`** (premisa sobre nacional superada por D31, que sí incorpora esa ventana -- ver ahí el tratamiento real de la ruptura Alianza→PJ): `especificacion_panel_temporal.md` §6.2 preveía una cuarta categoría de `continuidad_oficialismo`, `sin_oficialismo`, para "casos extremos (2001-2003, colapso del gobierno)" -- referencia a la crisis de diciembre de 2001 (renuncia de De la Rúa, sucesión de varios presidentes en dos semanas). Verificado en su momento que esa categoría **nunca se activaba** en el pipeline real, por dos motivos distintos según el nivel. A nivel **nacional**, la ventana 2001-2003 directamente no existía en el panel: `calendario_electoral.csv`/`oficialismo_por_nivel.csv` no cubrían nacional antes de 2011 (D15) -- `data/tfi_data/elecciones/2001_nacional.csv` a `2009_nacional.csv` existían pero estaban vacíos (solo encabezado), a diferencia de sus pares `municipal`/`provincial` (25 filas cada uno): no se había conseguido el escrutinio por lista a nivel Partido de La Plata para el cargo nacional en 2001-2009 (`docs/adquisicion_datos_especializacion.md` §1.a documenta el éxito de esa adquisición solo para `municipal`/`provincial`, en ese momento). El titular real de octubre de 2001 a nivel nacional era la ALIANZA (Fernando de la Rúa) -- dato correcto por completitud histórica, pero sin ninguna fila del panel a la que aplicarlo, hasta D31. A nivel **provincial** y **municipal**, en cambio, la ventana sí existe y el titular de octubre de 2001 (antes de la crisis de diciembre) está identificado sin ambigüedad y es continuo en los dos casos: provincial = PARTIDO JUSTICIALISTA (Carlos Ruckauf, gobernador desde dic-1999; renuncia en dic-2001 para ser canciller, lo sucede su vicegobernador Felipe Solá -- sucesión constitucional, no ruptura, y Solá gana la reelección en 2003 con lista propia); municipal = PARTIDO JUSTICIALISTA (Julio Alak, intendente desde 1991, reelecto en 1995/1999/2003) -- ambos ya codificados como `continua` en `_TITULAR_INICIAL_2001`/`construir_oficialismo_por_nivel` (`src/ml_models/construir_calendario.py`) y verificados contra las filas reales de `oficialismo_por_nivel.csv` (`2001,provincial,PARTIDO JUSTICIALISTA,...,continua` / `2001,municipal,PARTIDO JUSTICIALISTA,...,continua`). **Resolución**: `sin_oficialismo` se retira de la lista de valores esperados en `especificacion_panel_temporal.md` §6.2, documentada ahí como categoría planificada en el diseño original que nunca llegó a activarse -- no un valor vigente del pipeline. Los tres valores reales que produce `construir_calendario.construir_oficialismo_por_nivel` son `continua`/`continua_renombrada`/`ruptura`, confirmado contra las 34 filas de `oficialismo_por_nivel.csv` (ninguna categoría más aparece). No requiere cambios de código: es una corrección de la especificación contra el comportamiento ya correcto del pipeline | Adoptada |
| D30 | **Excepción puntual a la regla additive-only para una reorganización de archivos (rama `reorganizacion`, 2026-09-19)**: por pedido explícito de Iván, se autorizó mover/renombrar/borrar archivos tracked -- solo para esta tarea -- con garantías: tag `pre-reorganizacion` sobre HEAD antes de tocar nada, trabajo en rama dedicada, `git mv`/`git rm` siempre (nunca edición directa del árbol fuera de git), commits atómicos por fase, `pytest` en verde antes de cada commit de código. **Resultado**: `src/socioeconomia/geo.py` → `src/geolocalizacion/geo.py` (geometría pura -- circuito_id, radios censales, join espacial -- sin ninguna línea de ideología/V-Party; la premisa de que mezclaba responsabilidades no se sostuvo al leer el archivo completo, el problema real era solo la ubicación cross-domain). `src/analisis/vparty_cuadrantes_local.py` dividido: se borraron las 4 funciones deprecadas sin importador externo (`generar_distrito`/`graficar_cuadrantes_partido`/`_registros_json`/`main()`, más `RUTA_DISTRITO_DIR`/`_limites_globales`, sin uso ni test) y el resto (`tabla_distrito`/`tabla_localidades`/posiciones/filiaciones/color, protegido por D8 como análisis secundario) pasó a `src/analisis/vparty_localidad.py`. 63 comentarios de sección (`# ---`/`# N. Título`) borrados en 5 archivos (`generar_v_party_propio.py` + su test, `constantes.py`, `test_eph_client.py`), aplicando el skill de comentarios de `laplata-general`/`CLAUDE.md` al pie de la letra -- 0 docstrings tuvieron que tocarse en los 102 archivos de `src/`+`tests/` auditados, ya cumplían la regla. Rango temporal de `src/macroeconomia/` (CSV standalone, no el panel de `src/ml_models/`, que ya corría en 2001-2025 desde antes) extendido de 2011-2025 a 2001-2025 (`anio_inicio` default en `series.py`/`series_anuales.py`; ningún concepto del catálogo tiene piso real en 2011). `notebooks/ml/ventana_t-1/02_bayes.ipynb` reemplazado por su continuación (`02_bayes_2.ipynb`, reejecutada de punta a punta, resultado idéntico dentro del ruido de muestreo MCMC). Hallazgo colateral, no corregido en esta pasada (ver `data/macroeconomia/SISTEMATIZACION_VARIABLES_MACRO.md` §8): 10/20 conceptos de `catalogo_series.csv` tienen `cobertura_desde` más optimista que el dato real disponible en caché. Sin cambios de lógica de modelado en ningún paso (`pytest`: 619 passed en cada commit) | Adoptada |

| D31 | **Extensión de `nacional` a 2001-2009** (supera D15/D20 para este nivel; ver también correcciones a D4/D14/D18 arriba): iguala `nacional` a `provincial`/`municipal` -- 13 elecciones, 12 transiciones (`nacional_2001_2003` … `nacional_2023_2025`), habilitada por el backfill de `data/tfi_data/elecciones/2001-2009_nacional.csv` (antes solo encabezado) más la clasificación ideológica/V-Party correspondiente en `clasificacion_ideologica_agrupaciones.csv` (antes 0 filas para `nivel` ∈ {nacional, presidente} 2001-2009). Cambios en `construir_calendario.py`: `_FECHAS` toma la misma fecha que provincial/municipal para 2001/2003/2005/2007/2009 (elecciones concurrentes esos 5 años -- único desdoblamiento real del período es 2025, D4); `_ANIOS_EJECUTIVA_NACIONAL` agrega 2003 y 2007 (presidenciales reales); `_EJECUTIVA_PRE_2011["nacional"]` nueva, con 2003→JUSTICIALISTA/ruptura (Duhalde interino tras el colapso de dic-2001, Kirchner gana bajo esa etiqueta local) y 2007→AL. FTE. P/LA VICTORIA/continua (CFK sucede a NK); `_TITULAR_INICIAL_2001["nacional"]`/`CLASIFICACION_TITULAR_INICIAL["nacional"]` pasan de describir a CFK/2011 (remanente de cuando la ventana arrancaba ahí) a describir a la Alianza/De la Rúa en oct-2001, reusando la clasificación ya cargada de "AL. TRAB. JUST. EDUC. (1)" (confirmado por el usuario que es la Alianza -- el sufijo "(1)" de la fuente no se pudo verificar contra el PDF original, la herramienta de fetch de la sesión no accede a `web.archive.org`, no afecta la identificación). **Dato incierto, documentado, no descartado**: `data/tfi_data/elecciones/2003_nacional.csv` cita como fuente una URL fechada 14-sep-2003 (`BSAS_14_de_Sep_03/BastotPcial2003.pdf`, "Pcial" = provincial) pese a etiquetarse "(cargo: presidente)" -- podría corresponder a la legislativa de diputados nacionales (desdoblada de la presidencial del 27-abr en Buenos Aires) y no a la presidencial. Sin poder verificar contra la fuente primaria, se usa como único dato nacional disponible de 2003 bajo la fecha ya cargada en `_FECHAS` para ese año (2003-04-27, que a su vez podría ser un error preexistente de provincial/municipal -- fuera de alcance, no corregido). La conclusión de ruptura Alianza→PJ es la misma bajo cualquiera de las dos lecturas. `ALIAS_LISTA_OFICIALISMO` (`construir_resultado_distrito.py`) suma 3 entradas para des-ambiguar la etiqueta heredada de `oficialismo_por_nivel.csv` contra la boleta real de cada año: `(2005,"nacional")`→"AL. FTE. P/LA VICTORIA", `(2007,"nacional")`→"FTE. PARA LA VICTORIA", `(2009,"nacional")`→"FTE. JUSTICIALIS.P/LA VICTORIA". **Bug real encontrado y corregido, expuesto por esta extensión** (`_entrada_oficialismo`, código compartido por todos los niveles): comparaba `ganador.id_agrupacion == fila_oficialismo.id_agrupacion` para decidir si el ganador de la elección es el oficialismo -- los CSV de 2001-2009 nacional no traen `id_agrupacion` (a diferencia de municipal/provincial), así que `"" == ""` daba un falso positivo cuando ambas filas eran distintas y ninguna tenía id. Corregido a comparar identidad real de fila (`ganador is fila_oficialismo`); verificado contra las 39 filas `(año,nivel)` reales que el cambio solo afecta a `(2001, nacional)` (antes: `gana_oficialismo=True`/`share=31.34%`, los de JUSTICIALISTA; después: `False`/`19.34%`, los de la Alianza, que perdió esa legislativa) -- ningún resultado de municipal/provincial/nacional 2005-2025 cambia. `share_oficialismo`/`gana_oficialismo`/`delta_v` de `(2003, nacional)` quedan en blanco: la Alianza (titular entrante a esa elección) no tiene ninguna lista en esa boleta, sin importar cuál de las dos lecturas de fecha/cargo sea la correcta -- hueco real, no imputado, cubierto por `tests/ml_models/test_construir_distancias_ideologicas.py`/`test_resultado_distrito.py` (documentado explícitamente, no una regresión silenciosa). `continuidad_oficialismo(nacional_2003_2005)` da `"continua"` (no `"continua_renombrada"`: ese valor solo lo produce el código en años con elección ejecutiva propia, y 2005 es legislativo). **N efectivo de nacional en modelos que usan las 117 candidatas por default -- corregido tras re-ejecutar los 6 notebooks de `src/ml_models/` completos**: la estimación original de esta entrada (6→11 de 12 nominales) era **incorrecta**, basada en verificar solo la causa ya conocida (D3: la primera transición de cada nivel no tiene bloque largo `_vl`). La corrida real da **6→10** (de 12 nominales), igual que municipal/provincial (también `N=10`, mismo patrón, preexistente a esta extensión y nunca documentado hasta ahora): además de `nacional_2001_2003` (sin `_vl`, D3), se excluye también `nacional_2003_2005` -- no por un problema propio de esa ventana, sino porque `_delta_nivel`/`_delta_pendiente` (`features_ventana.py`, §5.3) se calculan contra el `_nivel_vc`/`_pendiente_vc` de la transición *anterior*, y varias series (EMAE, y las EPH-derivadas `tasa_informalidad`, `hacinamiento_medio`, `pct_hogares_*`) no tienen dato propio en absoluto para `nacional_2001_2003` (no solo para su `_vl`) -- el hueco de cobertura de la primera ventana se propaga un paso más allá de lo que D3 documentaba. No es un bug de código ni un hueco a imputar: es la consecuencia correcta de que esas series fuente no llegan tan atrás en el tiempo. **Columnas `_cobertura_parcial` cuasi-dummy**: sobre las 10 transiciones nacionales efectivas, `emae_cobertura_parcial` da `True` solo en `nacional_2005_2007` (antes se contaba también `nacional_2003_2005`, que ya no integra la muestra); `icg_cobertura_parcial`/`reservas_cobertura_parcial` eran `True` únicamente en `nacional_2003_2005`, así que dentro de las 10 filas efectivas quedan con varianza cero y los notebooks las descartan automáticamente (`[nacional] excluye columna(s) sin varianza: ['icc_cobertura_parcial', 'icg_cobertura_parcial', 'tc_oficial_cobertura_parcial']`) -- ya no llegan a competir en LASSO. Decisión original sin cambios: no se excluyen a mano del pool de candidatas (D28/D29 ya cubren varianza cero/NaN sin imputar); se reporta en los notebooks si LASSO selecciona alguna que sí sobreviva, con nota de cautela. **Efecto colateral aceptado, no causado por esta extensión**: al regenerar `elecciones.csv`/`distancias_ideologicas.csv`/`panel_ventanas.csv` se propagaron dos clasificaciones completadas en la misma sesión pero nunca antes regeneradas -- Humanista Ecologista (`2003_provincial.csv`, antes sin clasificar) resulta viable por el share que recalcula `construir_elecciones_resumen.py` (1.73%, contra 1.41% que lista el propio CSV con otro denominador) y entra a la dispersión ideológica de `(provincial, 2003)`; Proyecto Popular (`2005`) afecta `delta_posicion_ideologica` (sin piso de viabilidad) de las ventanas municipal/provincial que tocan 2003 o 2005 en sus extremos (`*_2001_2003`, `*_2003_2005`, `*_2005_2007`, 6 filas de `panel_ventanas.csv`). Verificado explícitamente contra baseline que ninguna otra fila de municipal/provincial cambia. **Invariante verificado en cada paso** (diff de contenido restringido a `nivel != "nacional"`, no sha256 de archivo completo -- varios archivos mezclan los tres niveles): `calendario_electoral.csv`/`oficialismo_por_nivel.csv`/`ventanas.csv`/`resultado_distrito.csv`/`voto_partido_distrito.csv` sin cambios fuera de nacional; `panel_trimestral_municipal.csv`/`panel_bieleccion_trimestral_municipal.csv` con hash idéntico al de antes de esta extensión. Efectos finales: `panel_ventanas.csv` 31→36 filas (151 columnas, sin cambio); bielección t-2 28→33 filas (nacional 6→11); `nacional_2013_2015` … `2023_2025` idénticas fila por fila | Adoptada (corregida, ver Corrección a D31) |

## Corrección a D31 (2026-09-21)

D31 documentaba `share_oficialismo`/`gana_oficialismo`/`delta_v` de
`(2003, nacional)` como hueco real, no imputado: la Alianza (titular
entrante a esa elección) no tiene ninguna lista en la boleta de 2003, y
el matching por nombre contra `oficialismo_por_nivel.csv` no tenía forma
de resolverlo. **Ya no es un hueco.**

En la misma sesión se auditaron y corrigieron a mano, en
`data/agrupaciones/oficialismos.csv`, las filas `nacional` 2001-2009 que
esa extensión había dejado con datos copiados sin verificar (`agrupacion_ganadora`/
`era_oficialismo`/`vparty_*` idénticos para 2003/2005/2007/2009,
pegados del resultado de 2003) -- entre ellas, `(2003, nacional)` pasó a
`PARTIDO JUSTICIALISTA,true,...`, curado contra el ganador real por
votos en La Plata (34.23%). Al re-ejecutar el pipeline (`ml_models.construir_calendario`
→ `construir_resultado_distrito` → `construir_elecciones_resumen` →
`construir_distancias_ideologicas` → `construir_panel_ventanas` →
`construir_panel_trimestral`/`construir_panel_bieleccion_trimestral`),
esto expuso un hecho no documentado hasta ahora: `_entrada_oficialismo`
(`construir_resultado_distrito.py`) consulta `oficialismos.csv` **sin
ninguna restricción de año** -- a diferencia de
`construir_calendario.construir_oficialismo_por_nivel`, que sí exige
`año >= 2011`. Con la fila curada de 2003 ya presente, `_entrada_oficialismo`
resuelve directo contra el ganador real de la elección (rama
`fila_of_curada is not None`, `era_oficialismo=true` → usa el ganador
por votos) sin pasar por el matching por nombre que fallaba con la
Alianza.

**Efecto verificado, no solo esperado**: `(2003, nacional)` pasa de
`gana_oficialismo`/`share_oficialismo` vacíos a `True`/`34.23%` en
`resultado_distrito.csv`/`elecciones.csv`; en `distancias_ideologicas.csv`
la fila `JUSTICIALISTA` de 2003 nacional pasa a `es_oficialismo=True`
(antes ninguna fila de ese año lo era); `delta_v` de `nacional_2001_2003`
y `nacional_2003_2005` en `panel_ventanas.csv` dejan de estar vacíos
(14.88 y 1.86 respectivamente). Las columnas `dispersion_economico_mu`/
`dispersion_progresismo_mu`/`magnitud_desplazamiento_ideologico` de
`(2003, nacional)` **no cambiaron** -- se calculan sobre todas las
fuerzas viables independientemente de cuál sea el oficialismo, así que
el conteo de `panel_ventanas.csv` sigue en 36 filas y la cobertura
mínima de V-Party (D18) sigue en 88.66%.

**Lo que esto NO cambia**: `nacional_2001_2003`/`nacional_2003_2005`
siguen excluidas del universo de modelado de LASSO (N=10, no 12) en
`01.1`-`01.3`/`03_desplazamiento_ideologico.ipynb` --
la causa que sí sigue vigente es que EMAE/las variables EPH-derivadas no
tienen dato propio antes de 2004-2005 (la causa original que esta misma
entrada de D31 ya documentaba), independiente de a quién se identifique
como oficialismo. El predictor H3 propio de `03_desplazamiento_ideologico.ipynb`
(`distancia_minima_al_oficialismo_t_menos_1`) sí pasa de estar disponible
en 11/12 transiciones nacionales a 12/12 -- pero como esa ventana igual
cae excluida por la causa EMAE/EPH, no cambia el `N` final reportado en
ese notebook.

Tests actualizados para reflejar esto (ya no documentan un hueco que no
existe): `tests/ml_models/test_construir_distancias_ideologicas.py::TestCasoRealAuditoria2023`
(`_SIN_OFICIALISMO_VIABLE_DOCUMENTADO` vacío, nueva
`test_2003_resuelve_justicialista_como_oficialismo`) y
`tests/ml_models/test_resultado_distrito.py::TestOficialismoNacionalResueltoContraDatosReales`
(sin exclusión de 2003, nueva `test_2003_gana_oficialismo_justicialista_34_23_pct`).
`pytest`: 624 passed (antes 622 -- los dos tests nuevos).

## Corrección a D17 (2026-09-13)

D17 documentaba que `votos_nulos`/`ausentismo` quedan vacíos para
`2025 provincial`/`2025 municipal` por un hueco real de fuente (la Junta
Electoral bonaerense no trae fila NULO ese año, ver Ley 5.109 en D19).
Se encontró que, para `ausentismo`, ese vacío tenía además **una causa
adicional evitable**: la rama fallback de `construir_elecciones` (años
sin `circuito_<cargo>.json`) exigía `votos_nulos` no-`None` para calcular
`ausentismo`, aunque `votantes_habilitados`/`votos_blancos`/
`votos_positivos` estuvieran completos --
`elif totales.get("nulo") is not None and totales.get("habilitados") is not None`.
Corregido tratando `nulo` como `0` **solo dentro de esa resta** (nunca en
la columna `votos_nulos` misma, que sigue vacía): `nulo =
totales.get("nulo") or 0`. Verificado con los dos casos reales del
período: `(provincial, 2025)` -- `habilitados=639839`,
`votos_positivos=393945`, `votos_blancos=15186` -- pasa de `ausentismo`
vacío a `ausentismo=230708`; `(municipal, 2025)` -- `positivos=395040`,
`blancos=14091`, mismo `habilitados` -- da el mismo `230708` (coincidencia
real: `positivos+blancos` suma `409131` en los dos niveles, mismo
padrón). `nacional 2025` no tenía este problema (`votos_nulos=8831`
presente, la Ley 5.109 es provincial). Se agregan además dos columnas
aditivas a `FilaEleccion`/`elecciones.csv`: `votos_blancos_y_nulos`
(`votos_blancos + (votos_nulos or 0)`) y `participacion_pct`
(`(votos_positivos + votos_blancos_y_nulos) / votantes_habilitados * 100`),
insumo directo de D19 -- el hallazgo surgió precisamente al diseñar esa
decisión, aunque D17 es anterior.

## Corrección a D6 (2026-09-06)

La columna booleana `periodo_intervenido` (`series_economicas_mensuales.csv`
y, agregada por trimestre, `panel_trimestral_<nivel>.csv`/
`panel_bieleccion_trimestral_<nivel>.csv`) se elimina: no llegó a
consumirse en ningún análisis de sensibilidad ni en el modelado -- una
columna sin lectores. La mitad de D6 que sigue vigente es la sustantiva:
el período 2007-2015 se incluye igual, sin excluirlo a priori: la reserva
sobre las series oficiales de ese tramo (`ipc`, `desocupacion`,
indirectamente `salario_real`) queda documentada en `nota_metodologica`
de `registro_variables.csv`, no en una columna del panel. Si en algún
momento se retoma el análisis de sensibilidad, la columna se puede volver
a derivar de las fechas (2007-01 a 2015-12) sin depender de este flag.

## Corrección a D4 (2026-09-01)

D4 asumía ventanas de largo homogéneo (~24 meses), con el único
desdoblamiento real en 2025. Verificado contra las 31 filas reales de
`ventanas.csv`: las ventanas municipal/provincial 2001-2011 (desdoblamientos
PBA de ese período, no solo 2025) van de 18 a 30 meses. D4 sigue vigente
como estaba para `panel_ventanas.csv` (que en efecto no normaliza por
duración de ventana, Fase 4) -- la homogeneidad de ~24 meses no es, sin
embargo, un hecho general del calendario electoral. El panel trimestral
(D13) sí normaliza, calculando N de trimestres por ventana real en vez de
asumir 8 fijo.

## Corrección a D12 (2026-09-01)

D12 documenta el mecanismo `_TITULAR_REAL_DIVERGE_DE_VOTO_LA_PLATA` con
un único caso real (gobernador 2019). Se encontró y corrigió un segundo
caso del mismo tipo, faltante hasta esta fecha: **presidente 2023**. La
Plata votó por UNION POR LA PATRIA en primera vuelta (`oficialismos.csv`,
`era_oficialismo=true`), pero Milei/ALIANZA LA LIBERTAD AVANZA ganó la
presidencia real en balotaje. Sin este caso en el diccionario, el titular
nacional no se actualizaba tras 2023 y la fila `2025,nacional` de
`oficialismo_por_nivel.csv` quedaba con `agrupacion_oficialismo=UNION POR
LA PATRIA`, arrastrado por error a `panel_ventanas.csv` y a
`panel_trimestral_nacional.csv` (Fase 5). Corregido agregando
`("nacional", 2023)` al diccionario -- `continuidad_oficialismo` de esa
fila pasa a `ruptura` (el Ejecutivo real sí cambió) y el titular desde
dic-2023 es ALIANZA LA LIBERTAD AVANZA. `gana_oficialismo`/
`share_oficialismo`/`delta_v` de `resultado_distrito.csv` no cambiaron --
ya eran correctos porque salen directo de `oficialismos.csv` curado, no
de `oficialismo_por_nivel.csv`.

## Nota sobre alcance real vs. especificado (auditoría de la rama)

La especificación (`especificacion_panel_temporal.md` §4.3) lista 8
variables económicas en estado `nucleo`. La auditoría inicial de esta rama
y los intentos de adquisición de la Fase 0.5 determinan el estado real de
cada una en `registro_variables.csv` — no se asume `nucleo` por default.
Igual criterio para la cobertura electoral 2001-2009: se documenta el
resultado real de los intentos de adquisición (ver sección "Datos
pendientes de adquisición" del reporte de la rama) en vez de asumir que el
panel alcanza los 31 valores de `delta_v` no nulos que la especificación
proyecta como ideal.

## Nota sobre `trayectorias_economicas*.py` tras D17

`src/visualizacion/trayectorias_economicas.py`/`trayectorias_economicas_bieleccion.py`
infieren qué columnas de `panel_trimestral_<nivel>.csv`/
`panel_bieleccion_trimestral_<nivel>.csv` son variables económicas a
graficar por descarte (`_COLUMNAS_FIJAS`, todo lo que no está en ese
set). Las columnas nuevas que trae D17 a esos dos CSV (`votos_positivos`,
`n_fuerzas_viables`, `dispersion_economico_mu`, etc.) no son variables
económicas y no tienen entrada en `_UNIDADES` -- sin ajustar
`_COLUMNAS_FIJAS` el payload las tomaba como si lo fueran y la pestaña
interactiva rompía (`unidades` vacío para esas columnas). Corregido
importando `ml_models.construir_elecciones_resumen.COLUMNAS_ELECCION_PANEL`
en `_COLUMNAS_FIJAS` de los dos módulos (y del test correspondiente) en
vez de repetir a mano la lista de columnas de `elecciones.csv` una tercera
vez -- mismo criterio D9 de no hardcodear lo que ya está declarado en otro
lado.

## Nota de seguimiento sobre D18 (pendiente, 2026-09-19)

La columna `distancia_oficialismo_alternativa` sigue en `panel_ventanas.csv`
y expuesta por `cargar_panel.py` (ver D18: comentada como DEPRECADA in situ
en `construir_panel_ventanas.py`, 17/31 filas vacías por el bug de matching
por nombre, sucedida por `distancias_ideologicas.csv`). Iván pidió
explícitamente dejarla señalada para una etapa posterior en vez de tocarla
en la reorganización de archivos (D30): sacarla del CSV/de `cargar_panel.py`
es un cambio de lógica de datos, no de estructura de archivos, y queda
fuera del alcance additive-only de esa rama.
